name: CI/CD to Minikube
on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t eval-rack-min:latest .

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Start Minikube
        uses: medyagh/setup-minikube@latest
      - name: Build image inside Minikube
        run: |
          eval $(minikube docker-env)
          docker build -t eval-rack-min:latest .
      - name: Create Kubernetes manifest
        run: |
          mkdir -p /tmp/devops-autopilot-k8s
          cat > /tmp/devops-autopilot-k8s/deployment.yaml <<'EOF'
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            name: eval-rack-min
          spec:
            replicas: 1
            selector:
              matchLabels:
                app: eval-rack-min
            template:
              metadata:
                labels:
                  app: eval-rack-min
              spec:
                containers:
                  - name: eval-rack-min
                    image: eval-rack-min:latest
                    imagePullPolicy: Never
                    ports:
                      - containerPort: 8000
                    readinessProbe:
                      tcpSocket:
                        port: 8000
                      initialDelaySeconds: 5
                      periodSeconds: 5
                    livenessProbe:
                      tcpSocket:
                        port: 8000
                      initialDelaySeconds: 15
                      periodSeconds: 10
          EOF
      - name: Deploy to Minikube
        run: |
          if [ -d k8s ]; then
            kubectl apply -f k8s/
          else
            kubectl apply -f /tmp/devops-autopilot-k8s/deployment.yaml
          fi
      - name: Expose app inside cluster
        run: |
          kubectl expose deployment eval-rack-min \
            --name=eval-rack-min \
            --type=ClusterIP \
            --port=80 \
            --target-port=8000 \
            --dry-run=client -o yaml | kubectl apply -f -
      - name: Wait for rollout
        run: kubectl rollout status deployment/eval-rack-min --timeout=120s
      - name: Smoke test deployed app
        run: |
          kubectl port-forward service/eval-rack-min 18080:80 > /tmp/eval-rack-min-port-forward.log 2>&1 &
          PF_PID=$!
          trap 'kill $PF_PID 2>/dev/null || true' EXIT

          for path in /health /health/ /; do
            for attempt in $(seq 1 20); do
              if curl -fsS "http://127.0.0.1:18080$path" >/tmp/eval-rack-min-response.txt; then
                echo "Smoke test passed on $path"
                exit 0
              fi
              sleep 2
            done
          done

          echo "Smoke test failed: app did not return a successful HTTP response."
          cat /tmp/eval-rack-min-port-forward.log || true
          kubectl get pods -o wide
          kubectl describe deployment eval-rack-min
          kubectl logs deployment/eval-rack-min --tail=100 || true
          exit 1
