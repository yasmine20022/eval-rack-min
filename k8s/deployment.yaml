apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval-rack-min
  labels:
    app.kubernetes.io/name: eval-rack-min
    app.kubernetes.io/instance: eval-rack-min
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eval-rack-min
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: eval-rack-min
        app.kubernetes.io/name: eval-rack-min
        app.kubernetes.io/instance: eval-rack-min
    spec:
      containers:
      - name: eval-rack-min
        image: eval-rack-min:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          failureThreshold: 30
          periodSeconds: 10
