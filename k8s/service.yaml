apiVersion: v1
kind: Service
metadata:
  name: eval-rack-min
  labels:
    app.kubernetes.io/name: eval-rack-min
    app.kubernetes.io/instance: eval-rack-min
spec:
  type: NodePort
  selector:
    app: eval-rack-min
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
